<form method="post" action="{{ path('app_reinscription_delete', {'id': reinscription.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reinscription.id) }}">
    <button class="btn">Delete</button>
</form>
