{% extends 'base.html.twig' %}

{% block title %}Détail des classes abstraite {% endblock %}
{% block titrePage %}Gestion des classes  {% endblock %}
{% block cardHeader %} {{ block ('title')}} {% endblock %}
{% block body %}
    <table class="table">
        <thead>
            <tr>
                <th>#</th>
                <th>promotion</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for etudiant in promotion_concrete.etudiantAnneeAcademiques %}
            <tr>
                <td>{{ loop.index }}</td>
                <td>{{ etudiant }}</td>
                <td>
                    
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_promotion_concrete_new') }}">Create new</a>

    <a href="{{ path('app_promotion_concrete_index') }}">back to list</a>

    <a href="{{ path('app_promotion_concrete_edit', {'id': promotion_concrete.id}) }}">edit</a>

    {{ include('promotion_concrete/_delete_form.html.twig') }}
{% endblock %}
