<form method="post" action="{{ path('app_frais_abstrait_delete', {'id': frais_abstrait.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ frais_abstrait.id) }}">
    <button class="btn btn-primary">Delete</button>
</form>
