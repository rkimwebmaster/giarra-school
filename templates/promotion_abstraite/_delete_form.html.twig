<form method="post" action="{{ path('app_promotion_abstraite_delete', {'id': promotion_abstraite.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ promotion_abstraite.id) }}">
    <button class="btn">Delete</button>
</form>
