<form method="post" action="{{ path('app_etudiant_annee_academique_delete', {'id': etudiant_annee_academique.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ etudiant_annee_academique.id) }}">
    <button class="btn btn-primary">Delete</button>
</form>
