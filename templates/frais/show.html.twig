{% extends 'base.html.twig' %}

{% block title %}Détail Frais{% endblock %}
{% block titrePage %}Gestion des frais {% endblock %}
{% block cardHeader %} {{ block ('title')}} {% endblock %}
{% block body %}
    <table class="table">
        <tbody>
            <tr>
                <th>#</th>
                <td>{{ loop.index }}</td>
            </tr>
            <tr>
                <th>Designation</th>
                <td>{{ frai.designation }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ frai.description }}</td>
            </tr>
            <tr>
                <th>DateButoire</th>
                <td>{{ frai.dateButoire ? frai.dateButoire|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_frais_index') }}">retour à la liste </a>

    <a href="{{ path('app_frais_edit', {'id': frai.id}) }}">edit</a>

    {{ include('frais/_delete_form.html.twig') }}
{% endblock %}
