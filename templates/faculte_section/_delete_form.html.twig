<form method="post" action="{{ path('app_faculte_section_delete', {'id': faculte_section.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ faculte_section.id) }}">
    <button class="btn">Delete</button>
</form>
